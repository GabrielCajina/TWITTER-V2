import { render } from "@testing-library/react";
import Tweets from "../components/Tweets";

describe("should render the tweets", () => {
  it("should render the tweets success", () => {
    // const tweet = render(<Tweets />);
  });
});
